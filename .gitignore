# Lean Lake build outputs (any depth)
**/.lake/

# Cargo / rustc build outputs (any depth)
**/target/

# experimental directories
hax/foo 
hax/issue-2017

# macOS Finder metadata (matches in any directory)
.DS_Store